-# .wrapper
-#   .contents
-#     = render "new"
-#     - @posts.each do |post|
-#       = render partial: "post", locals:{post: post}
.wrapper
  .contents
    = render "new"
    - @posts.each do |key, value|
      .post-date
        = key
      - value.each do |post|
        = render partial: "post", locals:{post: post}
